# Examples

These examples mirror the agent integration flows documented in `plaidly-docs`.

- `agent_integration.go` covers merchant registration, sandbox simulation, receipt lookup, and webhook verification.
